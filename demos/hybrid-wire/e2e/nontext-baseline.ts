/**
 * Known WCAG 1.4.11 / generated-content findings in this lab. Populated by the
 * central capture pass; empty until then.
 */
export const NONTEXT_BASELINE: Record<
  string,
  { ratio: number; required: number; unverified: boolean }
> = {
};
